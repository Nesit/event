<%= semantic_form_for [:admin, resource] do |f| %>
  <%= f.inputs do %>
    <%= f.input :title %>
    <%= f.input :text, as: :html_editor, input_html: { style: 'height: 525px;'} %>
    <%= f.input :started_at %>
  <% end %>
  
  <%= f.actions %>
<% end %>
